services:
  postgres:
    image: postgres:16-alpine
    container_name: practic_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: practic
      POSTGRES_PASSWORD: practic
      POSTGRES_DB: practic
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U practic"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: practic_redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
